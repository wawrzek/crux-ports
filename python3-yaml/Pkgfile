# Description:	Python interface to yaml
# URL:		http://pyyaml.org/
# Maintainer:   Wawrzek Niewodniczanski, main at wawrzek dot name
# Packager:	Alan Mizrahi, alan at mizrahi dot com dot ve
# Depends on:   python3 libyaml

name=python3-yaml
version=6.0.2
release=1
source=(https://github.com/yaml/pyyaml/archive/refs/tags/$version.tar.gz)

build() {
	cd pyyaml-$version
	${name%%-*} setup.py build
	${name%%-*} setup.py install --root=$PKG
	find $PKG -name '*egg-info' -exec rm -rf {} +

	rm -rf $PKG/usr/share
}
