# Description: efl libraries
# URL: http://www.enlightenment.org/
# Maintainer: Wawrzek Niewodniczanski, main at wawrzek dot name
# Packager: Victor Martinez, pitillo at ono dot com
# Depends on: dbus xorg-libxp xorg-libxscrnsaver gstreamer gst-plugins-base giflib libtiff libsndfile pulseaudio harfbuzz fribidi poppler librsvg bullet libraw libspectre luajit

name=efl
version=1.21.0
release=1
source=(http://download.enlightenment.org/rel/libs/$name/$name-$version.tar.xz)

build()
{
	cd $name-$version
	export CPPFLAGS=-I/usr/include/lua5.1
	./configure --prefix=/usr \
		--disable-systemd\
		--disable-nls\
		--enable-multisense\
		--enable-harfbuzz\
		--with-tests=none\
		--with-opengl=full\
		--disable-doc
	make
	make -j1 DESTDIR=$PKG install
}
