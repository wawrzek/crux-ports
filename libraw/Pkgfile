# Description:	Library for reading RAW files obtained from digital photo cameras
# URL:		http://www.libraw.org/
# Maintainer: Wawrzek Niewodniczanski, main at wawrzek dot name
# Packager:	Alan Mizrahi, alan at mizrahi dot com dot ve
# Depends on:	jasper

name=libraw
version=0.18.11
demosaicversion=0.18.8
release=1
source=(http://www.libraw.org/data/LibRaw-$version.tar.gz \
http://www.libraw.org/data/LibRaw-demosaic-pack-GPL2-$demosaicversion.tar.gz \
http://www.libraw.org/data/LibRaw-demosaic-pack-GPL3-$demosaicversion.tar.gz
)

build() {
	cd LibRaw-$version
	./configure \
		--prefix=/usr \
		--enable-lcms
	make
	make DESTDIR=$PKG install
}
